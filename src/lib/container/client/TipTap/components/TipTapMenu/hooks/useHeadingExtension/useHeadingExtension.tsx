"use client";

import { memo } from "react";

import { TipTapExtensionParams } from "../../../../TipTap.type";

const useHeadingExtension = memo(({}: TipTapExtensionParams) => {
  return <div>useHeadingExtension</div>;
});

export default useHeadingExtension;
