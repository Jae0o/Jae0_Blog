export interface AlertContextType {
  TAG: string;
  CATEGORY: string;
}

export interface AlertEditType {
  UPLOAD_SUCCESS: string;
  UPLOAD_FAIL: string;
}

export interface ErrorMessageTyps {
  SET_OPTION_LIST: string;
  GET_OPTION_LIST: string;
  SET_POST: string;
  GET_POST: string;
}
